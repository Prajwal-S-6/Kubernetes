apiVersion: v1
kind: Service
metadata:
  name: orders-web-service
  namespace: ecommerce-dev
  labels:
    env: dev
spec:
  type: ClusterIP
  selector:
    app: orders-web
    tier: frontend
  ports:
    - port: 80
      targetPort: 3000