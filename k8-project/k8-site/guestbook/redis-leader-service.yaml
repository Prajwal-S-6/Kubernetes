apiVersion: v1
kind: Service
metadata:
  name: redis-leader
  labels:
    app: redis
    role: leader
spec:
  selector:
    app: redis
    role: leader
    tier: cache
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
