apiVersion: v1
kind: Service
metadata:
  name: redis-follower
  labels:
    app: redis
    role: follower
spec:
  selector:
    app: redis
    role: follower
    tier: cache
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
