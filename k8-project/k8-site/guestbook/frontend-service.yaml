apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: dev
  labels:
    app: guestbook
    tier: frontend
spec:
  type: NodePort
  selector:
    app: guestbook
    tier: frontend
  ports:
    - port: 8080
      targetPort: 80
      nodePort: 32001